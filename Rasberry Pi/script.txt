import matplotlib.pyplot as plt

#Set empty lists
training_loss=[]
test_loss=[]

#Get the data from the history list
for i in range(len(history_to)):
  # Get training and test loss for each one of histories
  training_loss.append(history_to[i]['mean_absolute_error'])
  test_loss.append(history_to[i]['val_mean_absolute_error'])

# Create count of the number of epochs
epoch_count = range(1, len(training_loss) + 1)

# Visualize loss history
plt.plot(epoch_count, training_loss, 'r--')
plt.plot(epoch_count, test_loss, 'b-')
plt.legend(['MAE Training Loss', 'MAE Test Loss'])
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.show();